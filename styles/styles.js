import { StyleSheet } from 'react-native';

export default StyleSheet.create({

    playerInput: {
        borderWidth: 1,
        width: 200,
        height: 50,
        justifyContent: "center"
    },

    enterPlayer: {
        textAlign: "center",
        fontSize: 20
    }

});